-This is a Python library called nldate which is meant to deal with dates as natural language and convert them into date objects
-The environment is set up with uv. mypy, ruff, and pytest are all added as dev dependencies
-You will follow the Red-Green TDD paradigm, write test cases for all functions before writing the functions themselves, verify they go red, implement the function, and verify the tests pass

